"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
require("jasmine");
const load_previous_image_use_case_1 = require("./load-previous-image.use-case");
const mock_factory_1 = require("../Common/TestDoubles/mock-factory");
const use_case_type_1 = require("./use-case-type");
const no_images_error_1 = require("../Domain/no-images-error");
describe("LoadPreviousImageUseCase", () => {
    let useCase;
    beforeEach(() => {
        useCase = new load_previous_image_use_case_1.LoadPreviousImageUseCase();
    });
    it("should work", () => __awaiter(void 0, void 0, void 0, function* () {
        useCase.client = mock_factory_1.mockClient(use_case_type_1.UseCaseType.LoadPreviousImage);
        const previousImage = yield useCase.execute();
        expect(previousImage).toBeDefined();
    }));
    it("should work with a single image", () => __awaiter(void 0, void 0, void 0, function* () {
        useCase.client = mock_factory_1.mockClientHavingASingleImage();
        const previousImage = yield useCase.execute();
        expect(previousImage).toBeDefined();
    }));
    it("should throw with zero images", () => __awaiter(void 0, void 0, void 0, function* () {
        useCase.client = mock_factory_1.mockClientHavingNoImages();
        let error = null;
        try {
            yield useCase.execute();
        }
        catch (ex) {
            error = ex;
        }
        finally {
            expect(error).toBeInstanceOf(no_images_error_1.NoImagesError);
        }
    }));
});
