"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
require("jasmine");
const http_shim_1 = require("./http-shim");
const primitive_stubs_1 = require("../Common/TestDoubles/primitive-stubs");
const mock_factory_1 = require("../Common/TestDoubles/mock-factory");
mock_factory_1.mockHttpRequests();
describe("HttpShim", () => {
    let http;
    beforeAll(() => {
        http = new http_shim_1.HttpShim(primitive_stubs_1.emailHash);
    });
    it("should have endpoint", () => {
        expect(http.endpoint).toBeDefined();
    });
    it("should make rpc call", () => __awaiter(void 0, void 0, void 0, function* () {
        const response = yield http.rpc("");
        expect(response.status).toBe(200);
    }));
});
